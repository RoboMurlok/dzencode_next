# Copy this file to .env and adjust values as needed.
# Used by server/db.js to configure MySQL.

DB_HOST=db
DB_USER=root
DB_PASSWORD=root
DB_NAME=dzencode
DB_PORT=3306

# Optional: override server port
PORT=5000
